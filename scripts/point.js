function Point(abscisa, ordinata) {
    this.x = abscisa;
    this.y = ordinata;
}

/**
 * Return distance between this point and point
 * 
 * point    {Point}    Second point
 * return   {Number}   Distance in degrees
 */
Point.prototype.getDistance = function(point) {
    return null;
};